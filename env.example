# Configurações de Produção - Render
# Copie este arquivo para .env e configure os valores

# Debug mode (False para produção)
DEBUG=False

# Chave secreta do Django (GERE UMA NOVA!)
SECRET_KEY=your-super-secret-key-here-change-this

# Hosts permitidos (domínio do Render)
ALLOWED_HOSTS=*.onrender.com

# CORS - origens permitidas
CORS_ALLOWED_ORIGINS=https://*.onrender.com

# URL do banco de dados
DATABASE_URL=sqlite:///db/db.sqlite3

# ===========================================
# INSTRUÇÕES:
# ===========================================
# 1. Copie este arquivo: cp env.example .env
# 2. Edite o .env com seus valores reais
# 3. NUNCA commite o .env (está no .gitignore)
# 4. Configure as mesmas variáveis no dashboard do Render
